# lodestar — Consolidated Pause Review
**Date:** 2026-05-17 · **Reviewer:** Cascade (trainer + playwrighter + form-check lenses) · **Tier:** vibe-careful (reputational stakes) · **Scope:** whole codebase + plans + recruiter/HM fit + field saturation
**Evidence tags:** `[verified]` (followed link / read file in this turn), `[partially-verified]` (read snippet/summary), `[inferred]` (reasoned from adjacent evidence), `[speculative]` (intuition only), `[unknown]`.

---

## 0. TL;DR

- **State:** Scaffold + planning. Zero production code. README/PRIOR_ART/architecture plan are unusually strong for day 1; that's the asset. `[verified]`
- **Recruiter/HM fit:** Defensible *if* Tier-1 PQE artifacts (interviews + 2 priority reports + worked Playwright escalation + Cursor manifest) land. **The plan/scaffold alone is not yet a portfolio piece.** `[inferred]`
- **Field saturation:** Single-repo hobby triagers are crowded; cross-tool + human-severity + PQE-report-shape is still empirically a gap, but GitHub itself shipped a Copilot-SDK triage demo (IssueCrush) — raises the bar on the "novelty" claim. `[verified]`
- **Biggest risk:** Wei builds the pipeline (Tier-2) and runs out of energy before Tier-1 (interviews, reports, escalation) ships. Tier-2 is *not* the demonstration; Tier-1 is. `[inferred]`
- **Recommendation:** Resolve the two pending decisions (sampling strategy + Pass 5 rhythm), then **start Tier-1 work in parallel with Pass 5 finalization** — do not let pipeline gold-plating delay interview outreach.

---

## 1. Cleanup tasks (Cascade-side, completed/identified this turn)

| Item | Status | Notes |
|---|---|---|
| Verify rename `agentic-voc-bench` → `lodestar` cleared all paths | ✅ verified | `pyproject.toml`, `README.md`, `voc/__init__.py` all read clean; no stale references in repo files reviewed. `[verified]` |
| Confirm `tests/test_smoke.py` actually imports the four submodules cleanly | ✅ verified by inspection | All 4 stubs exist with `__init__.py`. Smoke would pass. `[verified]` |
| Verify CI advertised matches CI shipped | ⚠️ partial mismatch | Architecture plan claims `behave` step "will be added in Pass 5"; current `ci.yml` has ruff + mypy(advisory) + pytest only. **Not yet a bug — plan acknowledges it as planned. Flag for Pass 5.** `[verified]` |
| Confirm gitignore protects interview transcripts | ✅ | `interviews/raw/`, `interviews/transcripts/` ignored; README + template allow-listed. `[verified]` |
| Confirm LICENSE is MIT and attribution-clean | ✅ | MIT, no third-party code currently included. `[verified]` |
| Pending decisions tracker | ⏳ | Two open (sampling strategy A/B/C/D; Pass 5 rhythm). Surfaced in §10 below. |

No code edits made; review-only.

---

## 2. Codebase adversarial review (recruiter → HM → engineer)

### 2a. Recruiter 15-second scan `[inferred]`
- Repo name "lodestar" — recruiter-friendly, memorable, decodable.
- README headline ("Public, reproducible VoC pipeline for open-source agentic coding tools") is on-message for Cursor PQE-User-Ops JD ("Build and own an AI-native, first-of-its-kind Voice of the Customer program").
- "Status: v0 scaffold — in active build" is honest but **risky if the repo stays in this state when a recruiter clicks**. Recruiters generally don't return.
- **Defect R1 [verified]:** All Build-status rows except "Repo scaffold" are 🚧. A clicked-through recruiter today sees an honest empty repo. **This must change before any link is shared externally.**

### 2b. Hiring-manager 5-minute scan `[inferred]`
- `docs/PRIOR_ART.md` is the strongest single artifact in the repo. The synthesis ("severity-human-only carveout, structurally enforced via `severity_source: Literal['human']`") directly inverts a known anti-pattern in adjacent projects. This is the kind of thinking the JD asks for.
- `docs/superpowers/plans/2026-05-17-architecture.md` shows real engineering discipline (TDD overlay, BDD scenarios, falsifier table, assumption tax with explicit score 22/85). A senior HM at a tools company will recognize this as load-bearing rigor, not theater.
- **Defect HM1 [verified]:** No `docs/WRITEUP.md` exists yet, but the README says it's the intended entry point for HMs. A clicked HM will hit a dead link in their mental model.
- **Defect HM2 [verified]:** No worked example, no actual report, no actual interview synthesis. The plan is good; the proof is absent. HMs weight proof, not plans.

### 2c. Engineer 30-minute deeper look `[verified]`
- `voc/__init__.py` = 3 lines (version + docstring). Four submodule `__init__.py` files are placeholders.
- `tests/test_smoke.py` asserts `__version__ == "0.1.0"` and imports the four submodules. **It does not assert any behavior.**
- `pyproject.toml` deps are tight (httpx, pydantic v2, pandas, pyarrow, rapidfuzz, sklearn, tenacity). No bloat. Dev deps include `respx` for HTTP mocking — good sign for an engineer reading the file.
- `ci.yml` runs on `push` and `pull_request` to `main`; Python 3.11+3.12 matrix; cache pip; ruff + mypy(advisory) + pytest. **mypy advisory-only is a yellow flag** — for a "rigorous" portfolio piece an HM will expect either strict mypy or explicit "we'll tighten when modules land" note in README.
- **Defect E1 [verified]:** `pyproject.toml` lists `behave` in the architecture plan's stack but **not in dev-deps**. When Pass 5 adds BDD scenarios, dep install will fail.
- **Defect E2 [verified]:** No `respx`-based test exists yet to validate the HTTP mocking pattern the plan promises. Engineer will infer "claimed but not demonstrated."
- **Defect E3 [inferred]:** No `severity_source: Literal["human"]` schema yet. The strongest claim in PRIOR_ART.md is currently a promise, not a structural enforcement. **Land the schema stub early to back the claim.**

### 2d. Summary scoring (form-check vibe-careful tier)
| Section | Score | Min | Pass? |
|---|---|---|---|
| Headline (planning quality + prior art + scaffold) | 88 | 90 | ❌ −2 |
| Section 5 minima (evidence) | 80 | 80 | ✅ |
| Section 5 minima (falsifiability) | 90 | 85 | ✅ |
| Section 5 minima (assumption-tax discipline) | 78 | 70 | ✅ |

**Verdict: -2 below floor on headline. The gap is execution proof. Once one priority report + one worked escalation land, the headline clears 90 easily.**

---

## 3. Meta-review of §2 (did I check, or assume?)

| Claim in §2 | Verified? | Falsifier if I'm wrong |
|---|---|---|
| "Strongest artifact is PRIOR_ART.md" | Inferred from comparative reading — I'd need to A/B against alternatives | Could be wrong if HM weights scaffold-quality > prose |
| "Recruiters generally don't return" | `[speculative]` — common wisdom, not verified for Cursor specifically | A boutique-AI recruiter at hypergrowth Cursor may actually re-check |
| "mypy advisory-only is a yellow flag" | `[inferred]` from form-check conventions; some HMs don't care | Could be neutral signal at Cursor specifically |
| "behave not in dev-deps will break Pass 5" | `[verified]` — read `pyproject.toml`, ran grep mentally: `behave` not present | Trivially falsifiable: `grep behave pyproject.toml` → no match |
| "PRIOR_ART claim about gap is empirically defensible" | `[partially-verified]` — PRIOR_ART itself says some entries are `[partially-verified]` or `[inferred]` | GitHub's IssueCrush (verified this turn) tightens the "no public artifact" claim; lodestar's cross-tool + human-severity angle still holds, but the gap is narrower than written |
| "Tier-1 is the demonstration, not Tier-2" | `[inferred]` from JD language ("human judgment, escalation point, product expert") | JD also values infrastructure ("contribute to tooling that makes quality operations more scalable"). A reasonable HM could weight pipeline higher than I'm assuming. **Genuine uncertainty.** |
| "Scaffold-only repo today would lose a clicked recruiter" | `[speculative]` | Could be tested by Wei sending the link to a friendly recruiter for a 30-sec gut read |

**Things I did NOT do that a rigorous meta-review would do:**
- Did not grep for stale `agentic-voc-bench` references in `docs/` recursively
- Did not run `pytest` or `ruff` to verify they actually pass
- Did not verify the architecture plan's Pass 4.5 external-verification items have been done (they're listed as pending)
- Did not read `applications/cursor/strategy_cursor.md` v2 (the strategy doc) in this turn — I'm trusting the summary's characterization
- Did not check whether anyone has pushed to `main` since `f984d1b` (Day 1 scaffold commit)

These are real gaps. Listed in §10 as questions for Wei or follow-up Cascade tasks.

---

## 4. Playwrighter review (test plan, since no tests yet)

The architecture plan's §2.3 lists 15 pytest names + Gherkin scenarios for BDD. Playwrighter lens applied to the **Playwright-based worked escalation** (Day 6-7 in the moonshot plan):

### Strengths
- The plan correctly defers Playwright to a Day 6-7 escalation block (not architecture-blocking). Healthy scoping.
- Plan correctly anticipates web-surface-bug-may-not-exist fallback (pytest-CLI repro). Good failure-mode awareness.
- The `playwrighter` skill is named and queued for explicit invocation at Day 6 start. Aligns with the skill's "load fresh, don't pre-load" pattern.

### Weaknesses / risks
- **PW1 [verified]:** No actual Playwright dep is in `pyproject.toml` yet. When Day 6 starts, `pip install playwright` + `playwright install chromium` will be a cold start, not a continuation. Budget half an hour for first-time setup friction.
- **PW2 [inferred]:** The plan doesn't specify whether the Playwright repro test will live in `lodestar/` or in a separate fork of the target tool. Living in `lodestar/tests/escalation/` is the cleaner narrative ("here is my repro test in my VoC repo, linked from the bug report I filed"); living in a fork conflates concerns. **Recommend `lodestar/tests/escalation/test_<bug>.py` with a README pointing to the upstream bug.**
- **PW3 [inferred]:** No CI step is planned for the escalation Playwright test. That's defensible (one-shot demonstrator, not a continuous test), but the README should explicitly say so to avoid an HM thinking the test rotted.
- **PW4 [verified]:** Gherkin scenarios in §2.2 are for the priority-report consumer (read-flow), not for Playwright. So the BDD load runs through `behave`, and Playwright runs through a one-off pytest. **This is correct shape; just confirming.**
- **PW5 [speculative]:** The "find a web-surface bug in Continue/Cursor docs/Cline" gate is non-trivial. Aider is CLI-only. Cline is VS Code extension (limited public web surface). Continue has a web settings page. **Continue web settings is the only realistic target. If Continue doesn't yield a usable bug in 1-2h of hunting, pytest-CLI fallback is the right escape hatch — do not let perfect be enemy of good.**

### Test-pyramid health (against playwrighter principles)
- Unit pytest layer: well-scoped (15 named tests across 7 components). ✅
- Integration / contract layer: covered by respx-mocked HTTP tests. ✅
- BDD consumer layer: 6 scenarios covering the read-flow. Strong. ✅
- E2E Playwright layer: one worked-escalation test. Appropriately scoped. ✅
- Property/hypothesis tests: mentioned for schema validation. ✅
- Visual regression / accessibility: N/A — Markdown report renderer, not a UI. Correct omission.

**Playwrighter verdict: test plan is well-shaped; primary risk is execution (PW1, PW5).**

---

## 5. Trainer review (architecture pedagogy + plan-first iron-law compliance)

Trainer is always-on per user_rules. Applying its coaching + routing stance.

### Plan-first iron-law compliance (codified 2026-05-17 in trainer SKILL.md)
- ✅ Pass 1-4 complete before any production code (verified: only scaffold + plans + smoke test exist).
- ⏳ Pass 4.5 external verification not yet executed (GitHub TOS read, issue-count curl, LLM pricing reads).
- ⏳ Pass 5 TDD task synthesis not yet produced.
- **Coached note:** the plan-first iron law explicitly says plans are "revisable mid-journey when new evidence surfaces." This is correct framing — DO NOT use the iron law as a reason to refuse Tier-1 work (interviews, manifest writeup) in parallel with Pass 5. The iron law forbids *production code* before plan completion; it does not forbid *human judgment artifacts* (interviews, prose, recruitment outreach). **Tier-1 can start now.**

### Pedagogy / teaching deltas Wei should internalize
1. **"The plan is not the artifact."** The architecture plan is excellent. But HMs don't read architecture plans — they read READMEs and writeups, and they spot-check tests. Allocate writing-time accordingly.
2. **"Severity-source carveout" is your strongest single claim.** It's currently a sentence in PRIOR_ART.md and a planned pydantic Literal. Land the schema stub in Pass 5 as task #1 so the claim is backed by code, not prose. (~30 min Cascade work, high signal-to-effort.)
3. **Customer-interview recruitment is the critical-path risk.** The plan acknowledges this in kill-criteria, but recruitment is also the longest-lead human work. Wei should start recruitment DMs THIS WEEK regardless of Pass 5 status — interviews are inputs to the priority reports; reports are inputs to the writeup; writeup is the HM entry point. Reverse-engineer the timeline from here.
4. **The pedagogy question to ask yourself before each Cascade-scaffolded file lands:** *"Would I be able to explain every line of this file to a Cursor engineer in 5 minutes?"* If no, the file is AI-slop risk, not Wei-craft.

### Architecture-level concerns (trainer flag-and-route)
- **Trainer flag:** the 7-component pipeline (ingest → schema → dedup-fuzzy → dedup-semantic → moderation → ranker → classify → report) is honestly more pipeline than a 9-15h Wei-hours budget supports if any single component balloons. Recommend an explicit "dedup-semantic optional in v0; ship fuzzy-only if budget pinched" carveout. This was *not* in v3.1 of the moonshot plan.
- **Trainer routing:** if Pass 4.5 reveals a stop-the-world finding (GitHub TOS blocks redistribution; A2-A4 issue counts <50/week), don't push through. Route to `form-check` for an out-of-cycle re-scope.

---

## 6. Business review (recruiter / HM uptake; reframed from "buyout & adoption")

The original criteria for this project were:
- (A) Cursor PQE-User-Ops interview signal
- (B) Generalizable to other AI-quality / VoC / customer-feedback-engineering roles at developer-tools companies

### (A) Cursor-specific signal `[partially-verified]`
**Findings from web research this turn:**
- Cursor (Anysphere) team size estimates vary 50-300 employees; one source pegs ~150. $2B ARR. $50-60B valuation. 86 open roles. Hypergrowth, "extremely high hiring bar." `[verified — multiple sources]`
- Cursor has BOTH a "Software Engineer, User Operations" role (builds internal tooling/data infra/AI systems for support) AND the PQE-User-Ops role Wei is targeting. These are sister roles, not competitors. `[verified — cursor.com/careers]`
- Cursor explicitly cares about "voice-of-the-customer" — it appears in a Cursor Technical Support Engineer JD's skill list. `[verified — BeBee scrape]`
- **Implication:** The PQE-User-Ops role is part of a deliberate User-Ops org build-out. lodestar is on-message. The "buyout/adoption" frame (recruiter/HM uptake) is **realistic** if Tier-1 ships.

### (B) Generalizability `[inferred]`
- VoC commercial market (Nextiva, SentiSum, Zonka, Productboard, Canny) is mature but enterprise-survey-focused; **none mine public GitHub issues for product VoC**.
- "VoC engineer" as a role title at developer-tools companies is rare — closest analogs are "Customer Success Engineer," "Developer Experience Engineer," and "Product Quality Engineer." All are growing role categories but small absolute volume.
- **lodestar's generalizability beyond Cursor is real but narrow:** roles at GitHub, Sourcegraph, Replit, Continue, Codeium/Windsurf, Vercel, Linear, Notion. ~20-40 roles globally at any given moment, not hundreds.

### Recruiter screen weight
- Recruiters reportedly see "hundreds of MNIST classifiers and sentiment analyzers daily." Differentiation requires shipped-to-users + production-constraint signal. `[partially-verified — Markaicode 2026 article]`
- lodestar's differentiation lever is the **PQE-judgment artifact stack (interviews + reports + escalation),** not the Python pipeline. Pipeline is table-stakes; judgment is the moat.

### Adoption (will anyone use lodestar v0 besides Wei?)
- **Realistic: no.** v0 is a demonstration, not a product. Adoption is not the success metric. **Recruiter/HM uptake** is. Don't conflate.
- Stretch goal: HN/Reddit post drives 50-200 stars; a Continue/Aider maintainer comments. This is plausible but not load-bearing.

---

## 7. Field saturation (Palamedes-style fan-out summary)

| Category | Saturation | Threat to lodestar | Source |
|---|---|---|---|
| ML bug classification (Bugzilla, single-tracker) | Saturated — Mozilla bugbug owns it | Low; different shape | PRIOR_ART §1 `[verified]` |
| In-thread LLM issue assistants (per-repo, conversational) | Active, multiple projects (trIAge, IssueCrush) | **Medium** — GitHub's own IssueCrush (Copilot SDK demo, 2026) raises the bar | PRIOR_ART §2 + GitHub blog `[verified]` |
| Single-repo LLM issue prioritizers (CLI, hobby) | Thinly populated, low-star (1-star repos) | Low; lodestar diverges on cross-tool + human-severity | PRIOR_ART §3 `[verified]` |
| Customer-facing intake (ClearFlask, Productboard, Canny) | Saturated — commercial + 1 open-source | None; orthogonal problem (inbound, not outbound mining) | PRIOR_ART §4 `[verified]` |
| Bug-reproduction benchmarks (Defects4J, BugSwarm, SWE-bench) | Active academic field | None; downstream of triage | PRIOR_ART §5 `[verified]` |
| Cross-tool VoC synthesis for AI-coding-tools | **Empty** as far as verified searches show | This is lodestar's niche | PRIOR_ART headline `[partially-verified]` |
| PQE-portfolio-piece category specifically | Effectively empty (search results return resume sites, not artifacts) | This is lodestar's niche too | PRIOR_ART §gaps `[inferred]` |

**New threat surfaced this turn:** GitHub's "IssueCrush" Copilot-SDK demo (Andrea Griffiths, github.blog) is a React Native swipe-right triage app. It's per-issue (not cross-tool) and product-focused (not VoC-shaped), so the gap holds — but the existence of *first-party GitHub* prior art on AI-powered issue triage means lodestar's writeup must explicitly position against IssueCrush. **Add to PRIOR_ART.md in Pass 4.5.**

**Field-wide observation:** the "AI-powered issue triage" *concept* is mainstream. The differentiation is at the *shape* level (cross-tool, human-severity, PQE-report-output). Wei must hold this distinction tight in every recruiter touchpoint.

---

## 8. Synthesized to-do list (for Wei + Cascade to add to working list)

| # | Task | Owner | Effort | Priority | Blocks |
|---|---|---|---|---|---|
| 1 | Decide sampling strategy (A/B/C/D — see §10 Q1) | Wei | 5 min | P0 | Pass 4.5 → Pass 5 |
| 2 | Decide Pass 5 rhythm (A: push-through; B: checkpoint) (§10 Q2) | Wei | 5 min | P0 | Pass 5 start |
| 3 | Execute Pass 4.5 external verification (4 sub-tasks per arch plan §4.5) | Cascade | 30 min | P0 | Pass 5 |
| 4 | Synthesize Pass 5 (TDD task plan w/ tests) | Cascade | 1-1.5h | P0 | All code work |
| 5 | Land `voc/schema.py` with `severity_source: Literal["human"]` as Pass 5 task #1 | Cascade | 30 min | P0 | Backs strongest claim |
| 6 | Add `behave` to `pyproject.toml` dev-deps (defect E1) | Cascade | 2 min | P1 | BDD scenarios |
| 7 | Update README to remove `docs/WRITEUP.md` reference until file exists, or add placeholder (defect HM1) | Cascade | 5 min | P1 | Recruiter-clean-click |
| 8 | Update PRIOR_ART.md to add IssueCrush as new entry under Category 2 | Cascade | 15 min | P1 | Field-saturation accuracy |
| 9 | Draft customer-interview outreach DMs (Aider/Cline/Continue power users) | Wei (Cascade drafts template) | 30 min Wei + 15 min Cascade | P0 | Interviews → reports → writeup |
| 10 | Recruit ≥2 interview candidates this week | Wei | async, ~2h spread | P0 | Tier-1 critical path |
| 11 | Decide carveout: dedup-semantic optional in v0? (trainer flag §5) | Wei | 5 min | P1 | Pass 5 scope |
| 12 | Confirm interview consent template + recording setup before any interview | Wei | 30 min | P0 | Ethics gate |
| 13 | Form-check pre-score before Pass 5 tasks land (architecture plan §4.2) | Cascade | 15 min | P0 | Task code start |
| 14 | Week 1 hand-written priority report (after ingest + ranker land) | Wei | 1.5h | P0 | Demo content |
| 15 | Week 2 hand-written priority report | Wei | 1.5h | P0 | Demo content |
| 16 | Worked escalation via `playwrighter` (Days 6-7) | Wei + Cascade | 1.5-2h Wei | P0 | Demo content |
| 17 | Cursor product-familiarity manifest writeup | Wei | 0.5-1h | P1 | Writeup input |
| 18 | 2000-word writeup in Wei voice | Wei + Cascade | 1h Wei + 1.5h Cascade | P0 | HM entry point |
| 19 | 5-min demo recording | Wei | 1h | P0 | Cold-outreach hook |
| 20 | Form-check re-score; if ≥90 → public push | Cascade + Wei | 30 min | P0 | Ship gate |

**Sequencing note:** Tasks 1-3 are next 30 min. Tasks 9-12 can run in parallel with Pass 5 — DO NOT serialize.

---

## 9. Roadmap (where we are; where we're going)

```
[DONE]
 ├── Day 1 scaffold (LICENSE, README, pyproject, CI, voc/ stubs, smoke test) — commit f984d1b
 ├── PRIOR_ART.md (Categories 1-5 + synthesis + adopted-ideas + gaps)
 └── Architecture plan Pass 1-4 (S=22, pre-implementation; expected)

[CURRENT — this review]
 ├── REVIEW_2026-05-17.md (this doc)
 └── Wei decisions pending (sampling strategy; Pass 5 rhythm)

[NEXT 1-3 days]
 ├── Pass 4.5 external verification (Cascade)
 ├── Pass 5 TDD task plan + form-check pre-score (Cascade)
 ├── Customer-interview outreach DMs (Wei)
 ├── Add behave dep + IssueCrush to PRIOR_ART (Cascade cleanup)
 └── voc/schema.py with severity_source carveout (Pass 5 task #1)

[Days 3-5 calendar]
 ├── Customer interviews x 3-5 (Wei conducts)
 ├── Ingest + dedup + moderation + ranker land (Cascade implements, Wei reviews)
 └── Week 1 priority report (Wei writes)

[Days 6-7 calendar]
 ├── playwrighter skill invoked
 ├── Worked escalation Playwright repro (Wei + Cascade)
 └── Bug report filed upstream

[Days 8-10 calendar]
 ├── Week 2 priority report
 ├── Cursor manifest writeup (Wei from existing experience)
 ├── 2000-word writeup
 ├── 5-min demo recording
 ├── Form-check re-score (target ≥90)
 └── Public push if pass; soft-launch if marginal

[POST-SHIP]
 ├── Cursor PQE-User-Ops application + recruiter cold outreach
 ├── Optional HN/Reddit post (low priority; ship-blocking not on adoption)
 └── Generalize for sister-roles (GitHub, Sourcegraph, Continue, etc.)
```

---

## 10. Open questions for Wei (clearly marked)

> Each question is single-action. Answer inline; Cascade will execute.

**Q1 (P0): Candidate-ranker sampling strategy.** Architecture plan A2-A4 found Aider 13/wk, Cline 38/wk, Continue 93/wk — none hit the originally claimed n≥200/tool/week. Pick:
- (A) 4-week rolling window per tool (catches Aider at ~52, Cline ~152, Continue ~372) — current default
- (B) Lower per-week threshold to n≥50/tool/week and document limitation
- (C) Drop Aider; ship Cline + Continue only
- (D) Use 4-week window AND drop "per-tool floor" entirely; report whatever the corpus yields

**Q2 (P0): Pass 5 rhythm.**
- (A) Cascade pushes through Pass 4.5 + Pass 5 in one session (~2h Cascade work, no Wei checkpoint between)
- (B) Cascade ships Pass 4.5; Wei reviews; then Cascade starts Pass 5 (clean checkpoint; +1 turn overhead)

**Q3 (P1): Dedup-semantic carveout for v0?** Trainer flagged the 7-component pipeline is ambitious for 9-15h. Make dedup-semantic optional in v0? (Recommendation: yes — ship fuzzy-only; add semantic in v0.1 if budget permits.) Y/N?

**Q4 (P1): IssueCrush positioning.** GitHub's first-party Copilot-SDK triage demo is new prior art. Position lodestar against it explicitly in writeup as: (A) "complement to per-issue swipe triage with cross-tool weekly synthesis" or (B) ignore and let the reader infer? (Recommendation: A — name it; it raises credibility.)

**Q5 (P1): mypy strictness.** Current CI runs mypy advisory-only. Tighten to strict before Pass 5, or accept advisory-only and note in README? (Recommendation: tighten on `voc/schema.py` and `voc/ingest/github.py` only; leave rest advisory for v0.)

**Q6 (P0): Interview-consent template.** Do you already have a consent template you trust (Mailchimp/Intuit may have one not portable), or should Cascade draft one based on standard UX-research consent forms? Y = draft / N = bring your own.

**Q7 (P1): Public push gate.** If form-check re-scores at 87 (below 90 floor), do you:
- (A) Iterate once per form-check Section 5, then ship at whatever score lands
- (B) Hold ship until ≥90 even if it slips the 10-day window
- (C) Ship at any score ≥80 with explicit "v0 in progress" framing

**Q8 (P2): Writeup hosting.** Your own site? dev.to? hashnode? GitHub README? (Recommendation: README + a polished doc at `docs/WRITEUP.md` for the GH-native click-through; supplement with a dev.to crosspost for amplification.)

---

## 11. Things this review did NOT do (transparency)

- Did not run `pytest`, `ruff`, or `mypy` to verify CI claims actually pass — recommend Cascade does this in next turn before Pass 4.5.
- Did not deep-read `applications/cursor/strategy_cursor.md` v2 — trusted summary. If strategy doc has drifted from this review's framing, flag at next checkpoint.
- Did not check whether `~/.claude/skills/trainer/SKILL.md` mirror is in sync with canonical (last touched today; assumed sync).
- Did not assess the playwrighter skill's own state in `~/Projects/playwrighter/` for Day 6-7 readiness — directory exists with patterns/, references/, templates/, tools/, indicating it IS populated, but freshness not verified.
- Did not perform a full grep for stale `agentic-voc-bench` references in `lodestar/` recursively — spot-checks (README, pyproject, voc/__init__) clean.
- Did not verify any external links in PRIOR_ART.md resolve (Mozilla bugbug etc.) — high confidence by inspection.

These are reasonable next-pass items, not gaps in this review's verdict.

---

## 12. Verdict + ship-or-pivot recommendation

**SHIP** — continue per plan, with the deltas above. The project is on-thesis for the Cursor JD, the prior-art positioning is empirically defensible, and the planning rigor is unusually strong. The two pending decisions are 10-minute calls; the next 2-3 days are mostly Cascade scaffolding + Wei outreach in parallel.

**Pivot triggers (do not redefine mid-stream without form-check):**
- Pass 4.5 reveals GitHub TOS blocks even ID+URL+excerpt redistribution → re-scope to "we cite, we don't store" and add a writeup paragraph
- <2 interview recruits land by Day 5 → cut to 1 interview + 2 deep written-tool-use case studies
- Form-check re-score <85 after one iteration → soft-launch as "v0 in progress"

**Do NOT pivot for:**
- Pipeline component slipping (cut scope, don't repivot)
- One bad day of recruitment momentum
- Anxiety about IssueCrush or any other prior art — your positioning is differentiated; trust the work

---

## 13. Post-review addendum (2026-05-17 evening) — customer-access defect + v3.2 pivot

**Surfaced by:** Wei, immediately after reading this review.

**Defect:** Tier-1 customer-interview pillar is structurally infeasible.
- Real Mailchimp/Intuit customers → IP-locked to private repo; cannot ship to public `lodestar`.
- Aider/Cline/Continue user community → Wei has no relationship network for cold recruit.
- LLM-generated synthetic transcripts → AI-slop, recruiter-detectable, disqualifying.

**Missed by three prior reviews:**
- v2 form-check (caught wrong corpus, wrong shape, severity-classifier issue; missed access)
- v3.1 trainer-coached recalibration (caught budget unreality; missed access)
- §2-7 of this review (caught defects E1-3, PW1-5, IssueCrush threat; missed access)

**Pattern:** all three reviewers assumed customer-recruitment was tractable without checking the prior question — *whether Wei has any customer-access vector at all for a public repo*.

**Pivot (Wei-confirmed Option A):** drop the interview pillar entirely; public-source voice synthesis carries the voice slot. No methodology essay (priority reports already carry per-item rationale that demonstrates synthesis judgment; an essay would be a duplicate demonstration). No Wei-diary. No cold outreach. Tier-1 pillar count stays at 4 with one slot reshaped (interviews → public-source synthesis memos).

**Files patched as a result (this turn):**
- `applications/cursor/lodestar_plan.md` (formerly `moonshot_voc_pipeline_plan.md`) → v3.2 patch notice at top + body edits in §1.3, §1.4 (kill criteria hour 5 & 8), §2 (budget table + floor)
- `lodestar/README.md` → repo layout adds `voc/synthesis/`; build status swaps "Customer interviews" for "Public-source voice synthesis × 2 weeks (no live interviews)"
- `lodestar/.gitignore` → removed `interviews/raw/` and `interviews/transcripts/` ignores
- `lodestar/docs/superpowers/plans/2026-05-17-architecture.md` → Pass 4.5 adds Discord-archive-verification task
- `applications/.recovery/calibration.jsonl` → two entries appended (plan_revision + missed_detail)
- This addendum (§13) in this review doc

**Iron-law candidate (NOT yet codified):** *"Verify input-access-vector before scope-locking pillar deliverables."* Defer codification (IL-14 candidate) until pattern repeats — single occurrence is a learning event, not yet a discipline failure.

**Updated open questions:**
- Q6 (interview consent) → **OBSOLETE.** No interviews. Strike from §10.
- All other Q1-Q5, Q7-Q8 remain valid.

**Updated to-do list deltas (§8):**
- Tasks 9, 10, 12 → strike (interview outreach, recruitment, consent template)
- New task 9b: Cascade scaffolds `voc/synthesis/` + per-thread synthesis memo template (~30 min)
- New task 10b: Wei drafts Week 1 synthesis-memo methodology (which sources, sampling rule, anonymization rule) (~30 min)
- New task 11b: Verify Aider/Cline/Continue Discord channels are public-archived sources (cf. architecture plan Pass 4.5)

**Roadmap delta (§9):** "Customer interviews × 3-5 (Wei conducts)" line in Days 3-5 → replace with "Week 1 public-source voice synthesis memos (5-8 threads/tool, Wei writes)". Same calendar slot.

**Verdict unchanged: SHIP.** The pivot makes lodestar more honest, not weaker. The "I can do VoC with public-only signal" framing aligns with the JD's agentic + synthesis language. The previous framing required a customer-recruitment-yield that Wei had no path to, and would have been a failure mode by Day 5. Caught in planning, no shipped damage.

— end (v1.1, post-pivot) —
