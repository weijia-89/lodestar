# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Data (corpus pulls; reproducible from API)
data/raw/
data/cache/
*.parquet
*.jsonl
!tests/fixtures/*.parquet
!tests/fixtures/*.jsonl

# Secrets
.env
.env.local
*.key

# Editor
.vscode/
.idea/
.DS_Store

# Cost / API logs
.cost-log/
.api-cache/

# (v3.2 pivot) Interview transcripts removed — lodestar is public-source-only.
# Voice synthesis memos live in voc/synthesis/ and are git-tracked (public sources, no PII).

# mutmut generates a `mutants/` mirror tree during mutation testing runs.
# Regenerated on every `mutmut run`; not source.
mutants/
.mutmut-cache/
mutmut-stats.db

# Session handoffs, agent-to-agent context dumps, scratch.
# Never committed; reconstructable from the canonical docs.
localonly/

# Git worktrees for parallel-agent dispatch. Internal pointers, never source.
.worktrees/
